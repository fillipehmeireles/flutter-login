part of '../login_view.dart';

class _RegisterButton extends StatelessWidget {
  const _RegisterButton({super.key});

  @override
  Widget build(BuildContext context) {
    return PrimaryTextButton(text: 'Register', onPress: () => {});
  }
}
